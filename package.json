{
  "name": "socketio_dashboard",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Victor Hugo Negrisoli",
  "license": "MIT",
  "scripts": {
    "build": "mkdir dist && sucrase ./ --transforms imports -d ./dist",
    "startLocal": "set NODE_ENV=dev&& nodemon app.js",
    "startContainer": "nodemon app.js",
    "start": "set NODE_ENV=prod&& node dist/app.js",
    "heroku-prebuild": "rm -rf node_modules && rm -rf yarn.lock && yarn"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-handlebars": "^5.1.0",
    "mongoose": "^5.10.0",
    "socket.io": "^2.3.0",
    "nodemon": "^2.0.4",
    "sucrase": "^3.15.0"
  },
  "devDependencies": {
    "handlebars": "^4.7.6",
    "nodemon": "^2.0.4",
    "sucrase": "^3.15.0"
  }
}
